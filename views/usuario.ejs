<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login y Register</title>
  <link rel="stylesheet" href="/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/font-awesome.min.css">
  <link rel="stylesheet" href="/css/toastr.min.css">

  <script src="/js/jquery-3.5.1.min.js"></script>
  <script src="/js/bootstrap.bundle.min.js"></script>
  <script src="/js/toastr.min.js"></script>
  <script src="/js/bootbox.min.js"></script>
  <script src="/js/moment-with-locales.min.js"></script>
  <style>
    .card {
      display: inline-block;
      margin-left: 100px;
      text-align: center;
      padding-left: 10px;
      padding-right: 10px;
      min-width: 210px;
      vertical-align: top;
    }

    h1 {
      margin: 20px;
      text-shadow: 1px 2px 2px darkgoldenrod;
    }

    img {
      width: 190px;
      height: 230px;
    }

    #finalizar {
      vertical-align: bottom;
      margin-left: 50px;
    }

    .apu {
      max-width: 210px;
      margin-top: 40px;
    }
  </style>


  <script>

    $(document).ready(function () {

      moment.locale("es");

      function valida_envia() {
        //valido la edad. tiene que ser entero mayor que 18
        monto = document.fvalida.monto.value
        monto = validarEntero(monto)
        document.fvalida.monto.value = monto
        if (monto == "") {
          alert("Tiene que introducir un número entero en su monto.")
          document.fvalida.monto.focus()
          return 0;
        } else {
          <% for (let apuesta of apuestas.filter(x => x.producto == 1)) { %>
            if (monto < <%= apuesta.monto %>) {
          <% } %>



      alert("Debe ser un valor mayor")
    document.fvalida.monto.focus()
    return 0;
          }
        }

    //el formulario se envia
    alert("enviado");
    document.fvalida.submit();
      }


    toastr.options = {
      "closeButton": true,
      "progressBar": true,
    }

    bootbox.addLocale("pancho", {
      OK: 'Aceptar',
      CANCEL: 'Cancelar',
      CONFIRM: 'Confirmar'
    });

            <% for (let error of errors) { %>
      toastr.error("<%= error %>", "Error");
            <% } %>

            <% for (let mensaje of mensajes) { %>
      toastr.success("<%= mensaje %>", "Mensaje del Sistema");
            <% } %>
            
        });

  </script>

</head>

<body>

  <nav class="navbar navbar-expand-lg  sticky-top navbar-dark bg-warning">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><strong> <i class="fa fa-rebel" aria-hidden="true"></i> Sistema
        </strong></a>


      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <a class="navbar-brand">Welcome <%= user.name %></a>
      <div class="nav navbar-nav navbar-right">
        <a href="/logout" class="navbar-brand"><strong><i class="fa fa-sign-out" aria-hidden="true"></i>
            Logout</strong></a>
      </div>
    </div>
  </nav>

  <h1>Bid Now!</h1>

  <div class="card" style="width: 18rem;">
    <img src="/static/images/e65985207945dc92c19152479887f32c.png" class="card-img-top" alt="...">
    <div class="card-body">
      <h5 class="card-title">Grey Car </h5>
      <form name="fvalida" action="/newApuesta" method="POST">
        <input type="number" name="monto" placeholder="Enter your bet" required>
        <div>
          <input type="hidden" name="producto" value="1">
          <input type="submit" class="btn btn-outline-warning ccc" value="Bid">
        </div>
      </form>


      <table class="table align-middle order-table apu">
        <thead>
          <tr>
            <th scope="col"> Current Bids</th>
            <th scope="col"></th>
          </tr>
        </thead>
        <tbody id="currentBids">
          <% for (let apuesta of apuestas.filter(x=> x.producto == 1)) { %>
            <tr>
              <td>
                <%= apuesta.User.name %>
              </td>
              <td>
                <%= apuesta.monto %>
              </td>
            </tr>
            <% } %>
        </tbody>
      </table>

      <input class="form-control light-table-filter" data-table="order-table" type="text" placeholder="Search.." <=""
        form="">
    </div>
  </div>


  <div class="card" style="width: 18rem;">
    <img src="/static/images/bmw-x-series-x2-modelfinder-stage2-890x501.png" class="card-img-top" alt="...">
    <div class="card-body">
      <h5 class="card-title"> Yellow Car </h5>
      <form action="/newApuesta" method="POST">
        <input type="number" name="monto" placeholder="Enter your bet" required>
        <div>
          <input type="hidden" name="producto" value="2">
          <input type="submit" class="btn btn-outline-warning ccc" value="Bid">
        </div>
      </form>


      <table class="table align-middle order-table apu">
        <thead>
          <tr>
            <th scope="col"> Current Bids</th>
            <th scope="col"></th>
          </tr>
        </thead>
        <tbody id="currentBids">
          <% for (let apuesta of apuestas.filter(x=> x.producto == 2)) { %>
            <tr>
              <td>
                <%= apuesta.User.name %>
              </td>
              <td>
                <%= apuesta.monto %>
              </td>
            </tr>
            <% } %>
        </tbody>
      </table>

      <input class="form-control light-table-filter" data-table="order-table" type="text" placeholder="Search.." <=""
        form="">
    </div>
  </div>

  <div class="card" style="width: 18rem;">
    <img src="/static/images/aa3e553749e24b5afd28da96566483e7.png" class="card-img-top" alt="...">
    <div class="card-body">
      <h5 class="card-title">Red Car </h5>
      <form action="/newApuesta" method="POST">
        <input type="number" name="monto" placeholder="Enter your bet" class="ccc" required>
        <div>
          <input type="hidden" name="producto" value="3">
          <input type="submit" class="btn btn-outline-warning ccc" value="Bid">
        </div>
      </form>


      <table class="table align-middle order-table apu">
        <thead>
          <tr>
            <th scope="col"> Current Bids</th>
            <th scope="col"></th>
          </tr>
        </thead>
        <tbody id="currentBids">
          <% for (let apuesta of apuestas.filter(x=> x.producto == 3)) { %>
            <tr>
              <td>
                <%= apuesta.User.name %>
              </td>
              <td>
                <%= apuesta.monto %>
              </td>
            </tr>
            <% } %>
        </tbody>
      </table>

      <input class="form-control light-table-filter" data-table="order-table" type="text" placeholder="Search.." <=""
        form="">
    </div>
  </div>


  <a href="/finalizar"><button type="button" id="finalizar" class="btn btn-outline-secondary">End Bid</button></a>


  <script type="text/javascript">
    (function (document) {
      'use strict';

      var LightTableFilter = (function (Arr) {

        var _input;

        function _onInputEvent(e) {
          _input = e.target;
          var tables = document.getElementsByClassName(_input.getAttribute('data-table'));
          Arr.forEach.call(tables, function (table) {
            Arr.forEach.call(table.tBodies, function (tbody) {
              Arr.forEach.call(tbody.rows, _filter);
            });
          });
        }

        function _filter(row) {
          var text = row.textContent.toLowerCase(), val = _input.value.toLowerCase();
          row.style.display = text.indexOf(val) === -1 ? 'none' : 'table-row';
        }

        return {
          init: function () {
            var inputs = document.getElementsByClassName('light-table-filter');
            Arr.forEach.call(inputs, function (input) {
              input.oninput = _onInputEvent;
            });
          }
        };
      })(Array.prototype);

      document.addEventListener('readystatechange', function () {
        if (document.readyState === 'complete') {
          LightTableFilter.init();
        }
      });

    })(document);
  </script>
</body>

</html>